$(document).ready(function(){ // когда DOM будет готов
	
	// $('li').each(function(){
	// 	// console.log( $(this) .text() );
	// $(this).html('йцу<br>кен');
	// });
	 $("#owl").owlCarousel({
	 	items:4
	 });
});
